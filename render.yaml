services: 
  - type: web
    name: hh-parser
    env: docker
    paln: free
    envVars:
      - key: RAILS_MASTER_KEY
        sync: false
      - key: DATABASE_URL
        fromDatabase: 
          name: hh-parser-db
          property: connectionString

databases:
  - name: hh-parser
    plan: free

